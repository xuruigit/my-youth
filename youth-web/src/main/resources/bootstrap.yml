spring:
  application:
    name: youth-web
  cloud:
    config:
      uri: http://127.0.0.1:8083 #指定spring config server地址
      profile: dev               #指定环境
      label: master              #指定分支
      username: root
      password: root